# OVERVIEW

Used to configure all desktop computers in the CTTB

Customization to make a desktop cttb, dvbs or dvgs is managed with variables
(group_vars for groups such as dvbs or host_vars if a desktop needs some
special settings)

There are settings to customize anything needed, from browser to office
packages and if lightdm should show a list or users or not.
