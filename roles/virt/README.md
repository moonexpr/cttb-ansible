# Overview
Installs virtualbox, lxc or kvm based on specified variables. By default it
doesn't do anything

# LXC
## Adding a LXC controller
To add a host as a lxc controller (ie running containers), this host should end up in the lxc-host group in the _hosts_ file.
