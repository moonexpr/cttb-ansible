# Plank dock-items seed: day-1 default for the user's dconf user-db.
#
# Loaded by sudhanix-firstlogin's sfl_seed_user_dconf() on first login.
# Mirrors the system-db site default so first-login state is identical
# whether or not user-db is touched. Keep in sync with
# /etc/dconf/db/site.d/00-plank-dock1.
#
# auto-pinning=false stops Plank from silently promoting recently-
# launched apps to permanent launchers (and writing .dockitem files
# into ~/.config/plank/dock1/launchers/). Without this flag, dock-items
# would get clobbered on every Plank restart by Plank's own auto-detect.
#
# prefix: /net/launchpad/plank/

[docks/dock1]
dock-items=['xfce4-appfinder.dockitem', 'thunar.dockitem', 'thunderbird.dockitem', 'com.google.Chrome.dockitem', 'Zoom.dockitem', 'libreoffice-writer.dockitem', 'libreoffice-calc.dockitem', 'org.gnome.Calculator.dockitem', 'org.gnome.gedit.dockitem', 'gimp.dockitem', 'Trash.dockitem']
hide-mode='none'
auto-pinning=false
pinned-only=true
