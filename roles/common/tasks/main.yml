---

- name: Set platform/version specific variables
  ansible.builtin.include_vars: "{{ __common_vars_file }}"
  loop:
    - "{{ ansible_facts['distribution'] }}_{{ ansible_facts['distribution_major_version'] }}.yml"
  loop_control:
    loop_var: __common_vars_file_item
  vars:
    __common_vars_file: "{{ role_path }}/vars/{{ __common_vars_file_item }}"
  when: __common_vars_file is file

- name: Run shared setup tasks
  ansible.builtin.import_tasks: setup/default.yml

- name: Run platform-specific setup tasks
  ansible.builtin.include_tasks: "{{ item }}"
  loop: "{{ q('ansible.builtin.first_found', params, errors='ignore') }}"
  vars:
    params:
      files:
        - "{{ ansible_facts['distribution'] }}_{{ ansible_facts['distribution_major_version'] }}.yml"
      paths:
        - "{{ role_path }}/tasks/setup"
