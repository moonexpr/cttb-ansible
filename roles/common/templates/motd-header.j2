#!/bin/sh
#
# CTTB MOTD header — replaces Ubuntu default welcome.
# Primary doc: wiki.cttb. Upstream Ubuntu/XFCE docs are secondary.

printf "\n"
printf "  Welcome to %s on %s\n" "{{ sudhanix_description }}" "$(hostname)"
printf "\n"
printf "  Documentation\n"
printf "    Primary    http://wiki.cttb            (CTTB internal — start here)\n"
printf "    Upstream   https://help.ubuntu.com     (upstream base, secondary)\n"
printf "    XFCE       https://docs.xfce.org       (desktop environment, secondary)\n"
printf "\n"
