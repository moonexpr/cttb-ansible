---

- hosts: all
  become: yes
  gather_facts: no
  tasks:
    - name: Reboot host
      raw: /sbin/shutdown -r now

    - name: Monitor for reboot
      local_action: wait_for host={{ ansible_ssh_host }} port=22 state=stopped
