# CLAUDE.md

Project-specific rules. The global framework (~/.claude/CLAUDE.md) is loaded automatically.

---

## Navigation Rule (all modes)
**Always read `PROJECT.md` first** before any file lookups, grep, or glob. Use its subsystem table (paths and keywords) to navigate directly to the right files. Do not explore the repo blindly unless the repo has not been indexed in PROJECT.md.

---

## Project Files

| File | Purpose |
|------|---------|
| `CLAUDE.md` | Project-specific rules (this file). |
| `.claude/agents/` | Agent identity profiles. |
| `PROJECT.md` | Project conventions. Fill in during spec phase. |
| `WORKPLAN.md` | Session workplan. Created in spec, deleted in build. |
