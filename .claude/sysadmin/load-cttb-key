#!/bin/sh
# Compatibility forwarder. The sysadmin toolkit moved to utils/ so it is usable
# without reference to Claude; canonical path is utils/load-cttb-key.
exec "$(dirname "$0")/../../utils/load-cttb-key" "$@"
