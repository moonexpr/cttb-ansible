This file contains a list of improvements and other work required for ansible
that we're not tracking elsewhere because very code dependent

Each role has its own readme and todo file, consult those too
